pipeline {
    agent any
        stages {
            stage('1-Phil'){
                steps {
                    sh 'ps -ef'
                    sh 'sudo systemctl status jenkins'
                }
            }
            stage('2-processes running on the system-Abisola'){
			steps{
				sh 'ps -ef'
				sh 'sudo systemctl status jenkins'
			}
		}
        stage('3 christiane'){
            steps {
                sh 'ps -ef'
                sh 'sudo systemctl status jenkins'
            }
        }
        
    }
}
