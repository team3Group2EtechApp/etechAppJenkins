pipeline {
    agent any
        stages {
            stage('1-Phil'){
                steps {
                    sh 'ps -ef'
                    sh 'echo "sudo systemctl status Jenkins"'
                }
            }
        }
}
